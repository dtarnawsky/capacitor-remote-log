import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cap-remote-log',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
